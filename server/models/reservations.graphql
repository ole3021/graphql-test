scalar Date

type Reservation {
  id: ID
  guestName: String
  hotelName: String
  arrivalAt: Date
  departureAt: Date
}

type Query {
  getReservation(id: ID!): Reservation
  reservations: [Reservation]
}
